## Decouple to create: ImplicatorRegistry, MemoryRegistry
- make it possible to define a Registry decorator without defining
  methods that aren't being overridden (i.e. ProxyRegistry)

## Add serialization utilities
